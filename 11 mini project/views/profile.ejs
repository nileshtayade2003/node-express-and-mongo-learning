<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white p-10 min-h-screen">
  <div class="w-full flex justify-end">
    <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
  </div>
  <div class="flex item-start gap-3">
    <div class="w-10 h-10 rounded-md overlow-hidden">
      <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
    </div>
  <h3 class="text-3xl "><span class="font-light">Hello</span>, <%= user.name %> 👋</h3>
  </div>
  <h5 class="mb-5 mt-3">You can create a new post</h5>
  <form action="/post" method="post">
    <textarea name="content" id="" placeholder="What's on your mind?" class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md mb-2" ></textarea>
    <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md text-sm" value="Create New Post" type="submit">
  </form>

  <div class="posts mt-20">
    <h3 class="text-zinc-400">Your Post</h3>
    <div class="postcontainer mt-5">
      <% user.posts.reverse().forEach(function(post){ %>
        <div class="post w-1/3 p-4 mb-4 border-[1px] border-zinc-800 rounded-md bg-zinc-700">
          <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
          <p class="text-sm tracking-tight"><%= post.content %></p>
          <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
          <div class="flex gap-4 btns mt-2">
            <a href="/like/<%= post._id %>" class="text-blue-500 ">
              <%= post.likes.indexOf(user._id)===-1?"Like":"Unlike" %>
            </a>
            <a href="/edit/<%= post._id %>" class="text-zinc-600">edit</a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
